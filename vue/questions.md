# Вопросы по Vue.js для собеседования на Senior Frontend Developer

## 1. Основы Vue.js
1. Что такое Vue.js? Какие основные преимущества вы видите в использовании Vue по сравнению с другими фреймворками?
2. Какие основные принципы работают в Vue.js (двухстороннее связывание данных, декларативный подход, реактивность)?
3. В чем разница между `v-bind` и `v-model` в Vue?
4. Какие хуки жизненного цикла компонента Vue существуют и какие задачи они решают?
5. Как работает двухсторонняя привязка данных (two-way data binding) в Vue.js?

---

## 2. Компоненты
1. Что такое компоненты в Vue.js и какую роль они играют в приложении?
2. Как передавать данные между родительским и дочерним компонентами (использование `props` и событий)?
3. Как управлять состоянием компонента? Что такое `data`, `props` и `computed`?
4. Чем отличаются динамические компоненты (`<component :is="...">`) от стандартных?
5. Как можно реиспользовать код между компонентами (например, через миксины, композиционные API или HOCs)?

---

## 3. Директивы Vue.js
1. Что такое директивы Vue? Приведите примеры встроенных директив (например, `v-if`, `v-for`, `v-on`).
2. Чем `v-if` отличается от `v-show`? Когда применять каждый из них?
3. Как работает директива `v-bind`? В каких случаях её использовать?
4. Как настроить пользовательскую директиву? Приведите пример её создания.
5. Что такое модификаторы директив? Как они применяются?

---

## 4. Свойства компонента (Props, Computed, Watchers)
1. Что такое `props`? Как они передаются в дочерний компонент? Как валидировать `props`?
2. Чем `computed` свойства отличаются от методов? Какую роль они играют в оптимизации?
3. Для чего используются `watchers`? В каких случаях их использование оправдано?
4. Как работают `props` по умолчанию и какие ограничения на них накладываются?
5. Чем отличаются `methods` от `computed` и `watch`? Когда что использовать?

---

## 5. Роутинг (Vue Router)
1. Что такое Vue Router? Как настраивается маршрутизация в Vue?
2. Что такое "динамические маршруты" и как с ними работать (использование `:params`)?
3. Как настроить вложенные маршруты (nested routing) в Vue Router?
4. Что такое `navigation guards` в Vue Router и для чего они используются?
5. Как работает ленивый (lazy) и приоритетный (eager) загрузчик маршрутов?

---

## 6. Управление состоянием (Vuex/Pinia)
1. Для чего используется Vuex? В чем его слабые стороны и преимущества?
2. Объясните, что такое `state`, `getters`, `mutations` и `actions` в Vuex.
3. Чем отличается Vuex от Pinia? Какие преимущества Pinia дает в современных проектах?
4. Как работать с модулями Vuex для разделения состояния?
5. Как организовать модель асинхронного управления состоянием (например, использование `actions` для работы с API)?

---

## 7. Реактивность в Vue.js
1. Как устроена реактивность в Vue.js? В чем главное отличие от других фреймворков?
2. Что такое `reactive()` и `ref()`? Как они работают в Vue 3?
3. Чем отличается `proxy`-подход Vue 3 от `defineReactive` в Vue 2?
4. Как отслеживается изменение объектов и массивов в реактивных данных?
5. Что такое `watch` и `watchEffect`? Чем они отличаются?

---

## 8. Vue.js 3 и Composition API
1. Что такое Composition API и чем он отличается от Options API?
2. Как работает хук `setup()`? Какие задачи он решает?
3. Что такое `provide` и `inject`? Как эти методы помогают в передаче данных через дерево компонентов?
4. Как повысить читаемость компонентов с использованием Composition API?
5. Каким образом можно комбинировать Composition API и Options API в одном проекте?

---

## 9. Шаблоны (Templates) и Рендеринг
1. Как устроены шаблоны Vue? Чем они отличаются от обычного HTML?
2. Что такое слот (slot) в компонентах Vue? Чем отличается `v-slot` от стандартного `<slot>`?
3. Какие преимущества предоставляют динамические шаблоны?
4. Как реализовать условную логику в шаблонах Vue (например, с использованием `v-if`, `v-else-if`, `v-else`)?
5. Что такое рендер-функции (render functions)? Когда их использование оправдано?

---

## 10. Асинхронная работа
1. Как организовать асинхронный запрос данных с использованием Vue.js?
2. Какое поведение связывать с ленивой загрузкой компонентов?
3. Как обработать ошибки и статус загрузки в компоненте Vue (например, с использованием `try/catch`)?
4. Как реализовать автоматическое обновление данных в компонентах Vue?
5. Какие вы знаете способы отмены асинхронных запросов при изменении компонента?

---

## 11. Производительность Vue.js
1. Какие инструменты позволяют анализировать производительность Vue-приложений?
2. В чем разница между `v-if` и `v-show` с точки зрения производительности?
3. Как использовать `keep-alive` для повышения производительности Vue-приложений?
4. Что такое фрагменты (fragments) и как они помогают снизить нагрузку?
5. Какую роль играют мемоизированные вычисления, например, с использованием Vue 3 `shallowRef`?

---

## 12. Тестирование Vue.js
1. Какие библиотеки вы используете для тестирования Vue-компонентов? Чем отличаются Unit-тесты и End-to-End (E2E)?
2. Как написать тесты для компонента с использованием Vue Test Utils?
3. Как тестировать компоненты с использованием методов жизненного цикла?
4. Какой подход вы используете для мокирования Vue Router или Vuex в тестах?
5. Какие инструменты помогают тестировать рендеринг и события в Vue.js?

---

## 13. Полезные практики и антипаттерны
1. Какие антипаттерны Vue-приложений вы знаете? Как их избегать?
2. Как эффективно разделять логику между компонентами?
3. Почему важно избегать слишком большого количества `watcher`-ов?
4. Какие техники вы используете для предотвращения избыточного ререндеринга компонентов?
5. Как организовать проект Vue.js для поддержки масштабируемости и читаемости?

---

## 14. SSR (Server-Side Rendering) с Vue.js
1. Что такое SSR? Почему он полезен для SEO и производительности?
2. Как Vue поддерживает SSR? В чем основное отличие SSR от SPA?
3. Как работает `Nuxt.js` для серверного рендеринга?
4. Какие задачи можно решить при помощи CSR (Client-Side Rendering) в SSR приложении?
5. Что такое гидратация (hydration) и зачем она нужна в приложениях с SSR?

---

Эти вопросы охватывают все ключевые аспекты Vue.js, включая как базовые концепции, так и продвинутые инструменты и подходы, которые необходимы для работы на уровне Senior Frontend Developer.
