# Вопросы по RxJS для собеседования на Senior Frontend Developer

## 1. Основы RxJS
1. Что такое RxJS, и для чего она используется?
2. Объясните, что такое Observable. Чем он отличается от Promise?
3. Что такое Observer? Как он взаимодействует с Observable?
4. Что такое "unicast" и "multicast", и как это реализуется в RxJS?
5. Какие основные классы предоставляет RxJS, помимо Observable?

---

## 2. Основные концепции
1. Что такое "стрим" (stream) в контексте RxJS?
2. Чем отличается горячий Observable (hot) от холодного (cold)?
3. Объясните следующие термины:
  - Подписка (Subscription)
  - Оператор (Operator)
  - Поток (Stream)
  - Subject
4. Какие этапы проходят данные в стриме (три функции Observable: next, error и complete)?
5. Как завершить работу Observable? Какие механизмы для этого предоставлены?

---

## 3. Операторы RxJS
### 3.1 Классификация операторов
1. Какие виды операторов в RxJS вы знаете? Перечислите их и объясните классические примеры (filtering, transformation, combination и т.д.).
2. Как работают операторы создания, например, `of`, `from`, `interval`, `timer`?
3. Какие фильтрующие операторы вы используете (например, `filter`, `first`, `last`, `take`, `skip`)? Приведите примеры.
4. Объясните разницу между трансформирующими операторами: `map`, `switchMap`, `mergeMap`, `concatMap` и `exhaustMap`.
5. Что делают операторы комбинации, например, `combineLatest`, `forkJoin`, `zip`?

### 3.2 Особенности некоторых операторов
1. Какими методами можно реализовать объединение потоков (`merge`, `concat`, `combineLatest`)? Когда использовать каждый из них?
2. Как работает `debounceTime` и `throttleTime`? Когда их стоит применять?
3. Чем `map` отличается от `switchMap` в контексте запросов к серверу?
4. Чем отличается `catchError` от `retryWhen`? Как они помогают в обработке ошибок?
5. Для чего используется оператор `share`? Какие подводные камни могут быть при его использовании?

---

## 4. Subjects
1. Что такое Subject? Чем он отличается от Observable?
2. Как работают следующие типы Subject-ов:
  - `Subject`
  - `BehaviorSubject`
  - `ReplaySubject`
  - `AsyncSubject`
3. В каких случаях стоит использовать конкретный Subject?
4. Почему применение Subject является потенциально опасным? Какие правила важно соблюдать?
5. Как реализовать подписку на Subject и одновременно предотвращать утечки памяти?

---

## 5. Управление подписками
1. Как управлять подписками в RxJS? Почему важно освобождать подписки?
2. В чем разница между использованием `Subscription.unsubscribe` и оператора `takeUntil`?
3. Что такое `finalize`, и как он помогает при завершении работы Observable?
4. Как использовать `switchMap` для автоматического отмены предыдущих подписок?
5. Какие есть лучшие практики для предотвращения утечек памяти в RxJS?

---

## 6. Асинхронная работа с данными
1. Как интегрировать RxJS с асинхронными источниками (например, REST API)?
2. Чем стримы в RxJS отличаются от нативных Promises для управления асинхронностью?
3. Как можно объединить несколько асинхронных потоков данных (например, запросы к разным API)?
4. Как обрабатывать ошибки в асинхронных потоках (например, с использованием `catchError`, `retry`)?
5. Что сделать, чтобы данные из Observable обновлялись автоматически, если источник изменился? Приведите пример.

---

## 7. RxJS и Angular
1. Как RxJS интегрируется с Angular? Какие его функции используются повсеместно?
2. Чем полезен паттерн "Observable data streams" в Angular-сервисах?
3. Как вы используете `RxJS` для работы с данным сервисом Angular (например, `HttpClient`)?
4. Почему часто используют `async` пайп для подписки на Observable в Angular-шаблонах?
5. Как использовать `BehaviorSubject` или `ReplaySubject` для управления состоянием приложения в Angular?

---

## 8. Debugging и тестирование RxJS
1. Какие подходы вы используете для отладки Observable (например, `tap`, `console.log`, DevTools)?
2. Как написать мок для Observable в тестах?
3. Как тестировать цепочку операторов RxJS? Какие инструменты вы используете?
4. Чем полезен `TestScheduler`, предоставляемый RxJS, при написании тестов?
5. Как можно упростить процесс тестирования потоков данных?

---

## 9. Производительность RxJS
1. Какие обычные ошибки приводят к проблемам производительности при работе с RxJS?
2. Как оптимизировать работу с большими потоками данных?
3. Как влияет на производительность большое количество подписчиков на Subject?
4. Какие есть подходы к минимизации количества событий в потоках? Когда это важно?
5. Как использовать мемоизацию или кеширование данных в RxJS для повышения производительности?

---

## 10. Продвинутые темы
1. Что такое "Higher-Order Observables"? Как с ними работать?
2. Чем отличаются `multicast`, `publish` и их варианты (например, `publishReplay`)?
3. Как реализовать механизм "backpressure" при обработке большого объема данных?
4. Что такое оператор `groupBy`, и для каких задач он используется?
5. Какую роль играет Scheduler в RxJS?

---

## 11. Современные возможности RxJS
1. Какие изменения появились в последних версиях RxJS (например, v7)?
2. Как встроенная поддержка `rxjs-ajax` помогает при работе с HTTP?
3. Чем полезен `rxjs/testing` для организации тестов?
4. Как работать с WebSocket-соединениями в RxJS?
5. Какие полезные сторонние библиотеки для RxJS вы знаете? Когда их стоит использовать (например, `rxjs-spy`)?

---

Эти вопросы покрывают как базовые знания и продвинутые техники работы с RxJS, так и его использование в реальных приложениях, включая Angular.
