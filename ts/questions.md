# Typescript questions
1. Какие типы?
2. Что такое generic?
3. Что за вспомогательные типы Record, Omit, Partial?
4. Что такое декораторы и писал ли свои?
5. Чем unknown отличается от any?
6. Что такое интерфейс?
7. Что такое абстрактный класс?
8. Можно ли наследоваться от нескольких классов?

# Вопросы по TypeScript для собеседования на Senior Frontend Developer

## 1. Основы TypeScript
1. Что такое TypeScript и чем он отличается от JavaScript?
2. Какие преимущества использования TypeScript вы могли бы назвать?
3. Чем отличаются явная типизация в TypeScript и динамическая типизация в JavaScript?
4. Что такое интерфейс (interface) в TypeScript и как он используется? Чем он отличается от `type`?
5. Какие существуют опции компилятора TypeScript (файл `tsconfig.json`)? Объясните работу ключевых флагов, таких как `strict`, `noImplicitAny`, `target`.

---

## 2. Типизация
1. Какие бывают базовые типы данных в TypeScript? Приведите примеры.
2. Что такое `any`, `unknown`, `void`, `never`? В каких ситуациях использовать?
3. Как работают кортежи (tuple) в TypeScript? Приведите пример использования.
4. Чем отличается `|` (union) от `&` (intersection)?
5. Что такое "приведение типов" (type assertion)? Как оно используется?

---

## 3. Интерфейсы и типы
1. В чем разница между `interface` и `type`? Когда использовать каждый из них?
2. Как реализуется наследование интерфейсов? Приведите пример.
3. Как объединить один или несколько типов (type intersection)?
4. Чем полезен оператор `keyof` при работе с типами?
5. Что такое индексные сигнатуры в интерфейсах? Как их использовать?

---

## 4. Классы
1. Какова роль `class` в TypeScript? В чем разница между `public`, `private`, `protected`?
2. Что такое абстрактные классы и методы? Приведите пример их использования.
3. Чем `readonly` свойства в классах отличаются от `const`?
4. Как наследовать класс от другого класса? Как работать с переопределением методов?
5. Что такое "декоратор" в TypeScript и как он используется (например, `@Component` в Angular)?

---

## 5. Генерики (Generics)
1. Что такое Generics в TypeScript? Приведите пример их использования.
2. Как ограничить тип для Generics с помощью конструкций `extends`?
3. Чем полезны Generics для создания универсальных функций и классов?
4. Как работают дженерики в функциях, интерфейсах и классах?
5. Каковы преимущества объединения Generics с `keyof`?

---

## 6. Модули
1. Как работают модули в TypeScript? Чем отличаются `import` и `export`?
2. Объясните разницу между `export default` и именованным экспортом.
3. Чем отличаются относительные и абсолютные импорты? Как их настроить через `tsconfig.json`?
4. Что такое декларации модулей (`declare module`)? В каких ситуациях они применяются?
5. Как избежать циклических зависимостей в TypeScript-модулях?

---

## 7. Типы данных высокого уровня
1. Что такое пользовательские типы (custom types) в TypeScript? Приведите пример.
2. Как правильно использовать тип `Partial<T>`? Какие ещё утилитарные типы существуют (`Pick`, `Omit`, `Readonly`)?
3. Что такое "mapped types"? Объясните работу с ними.
4. Как работает `Record<K, T>`? Для чего он используется?
5. Как использовать `infer` для создания сложных типов?

---

## 8. Функции
1. Как типизировать функции в TypeScript? Приведите пример с типами аргументов и возвращаемого значения.
2. Чем отличается использование `this` в функциях TypeScript от JavaScript?
3. Что такое перегрузка функций? Как её реализовать в TypeScript?
4. Как работают расширенные функциональные типы (например, обратный вызов с аргументами)?
5. Как типизировать функцию с необязательными аргументами и аргументами по умолчанию?

---

## 9. Обработка ошибок
1. Как типизировать `try-catch` блоки в TypeScript?
2. Чем отличается типизация ошибок в синхронном и асинхронном коде?
3. Как типизировать функции, которые выбрасывают исключения?
4. Как использовать `never` в случаях обработки ошибок?
5. Как облегчить типизацию ошибок, возвращаемых в API (например, от Axios)?

---

## 10. Работа с библиотеками
1. Что такое декларационные файлы (`.d.ts`)? Когда они используются?
2. Как работает генерация типов для JavaScript-библиотек без официальной поддержки TypeScript?
3. Как установить и использовать сторонние типы из DefinitelyTyped (например, `@types/lodash`)?
4. Чем отличается `--skipLibCheck` при компиляции TypeScript?
5. Каковы основные подходы к написанию своих декларативных типов для существующих библиотек?

---

## 11. Асинхронный код
1. Как типизировать функции, возвращающие `Promise`?
2. Какие типы используют в асинхронном коде для обработки данных API?
3. Как использовать `async/await` с типами в TypeScript?
4. Как типизировать данные, возвращаемые из `fetch` или `axios`?
5. Что такое `Awaited<T>`? Как он используется?

---

## 12. Интеграция с JavaScript
1. Как TypeScript работает с чистым JavaScript-кодом? Какие ограничения существуют?
2. Что такое `allowJs`? Когда его стоит включать?
3. Чем `declare` позволяет указывать типы для уже существующих JS-объектов?
4. Как настроить строгую проверку типов для стороннего JavaScript-кода?
5. Что такое `checkJs`, и как он улучшает миграцию с JavaScript на TypeScript?

---

## 13. Инструменты и оптимизация
1. Как настроить TypeScript для работы в проекте? Какие опции нужно включить для строгой типизации?
2. Как проверить соответствие типов во время сборки проекта?
3. Как интегрировать TypeScript с сборщиками, такими как Webpack, Rollup, Vite?
4. Что такое "declaration files" (`.d.ts`), и когда их нужно генерировать?
5. Как производить рефакторинг TypeScript-кода, минимизируя ошибки?

---

## 14. Тестирование типов
1. Что такое "Contract testing" на уровне типов? Как его реализовать?
2. Как вы пишете Unit-тесты для TypeScript-классов и функций?
3. Какие библиотеки используются для тестирования типов (например, `tsd`)?
4. Как вы тестируете использование собственных утилитарных или сложных типов?
5. Какие стратегии отлова ошибок типизации в сложных проектах вы используете?

---

## 15. Работа с React и TypeScript
1. Как использовать TypeScript с React-компонентами? Как типизировать `props` и `state`?
2. Как типизировать функциональный компонент с использованием `React.FC`?
3. Как решить проблемы с типизацией `defaultProps` в React?
4. Как использовать Generics в типизации компонентов (например, HOC)?
5. Чем типизация контекста (`React.Context`) отличается от типизации пропсов?

---

Эти вопросы охватывают широкий спектр знаний и навыков, которые помогут проверить понимание TypeScript на уровне Senior Frontend Developer.
